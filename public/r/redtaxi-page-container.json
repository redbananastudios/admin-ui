{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "redtaxi-page-container",
  "title": "Red Taxi Page Container",
  "author": "Red Banana Studios",
  "description": "Standalone Red Taxi page container admin component.",
  "dependencies": [
    "clsx",
    "tailwind-merge"
  ],
  "files": [
    {
      "path": "src/lib/utils.ts",
      "content": "import { clsx, type ClassValue } from \"clsx\"\nimport { twMerge } from \"tailwind-merge\"\n\nexport function cn(...inputs: ClassValue[]) {\n  return twMerge(clsx(inputs))\n}\n",
      "type": "registry:file",
      "target": "src/lib/utils.ts"
    },
    {
      "path": "src/components/admin/page-container.tsx",
      "content": "import type { PropsWithChildren } from \"react\";\nimport { cn } from \"@/lib/utils\";\n\ntype PageContainerProps = PropsWithChildren<{\n  className?: string;\n}>;\n\nexport function PageContainer({ children, className }: PageContainerProps) {\n  return (\n    <div\n      className={cn(\n        \"mx-auto flex w-full max-w-7xl flex-1 flex-col gap-6 px-4 py-5 sm:px-6 lg:px-8 lg:py-7\",\n        className,\n      )}\n    >\n      {children}\n    </div>\n  );\n}\n",
      "type": "registry:file",
      "target": "src/components/admin/page-container.tsx"
    }
  ],
  "meta": {
    "brand": "redtaxi",
    "scope": "standalone-admin"
  },
  "docs": "Install this item when you need the Page Container component without pulling in the full Red Taxi admin bundle.",
  "categories": [
    "admin",
    "standalone",
    "component"
  ],
  "type": "registry:block"
}